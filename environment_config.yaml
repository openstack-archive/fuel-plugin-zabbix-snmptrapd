attributes:
  metadata:
    restrictions:
      - condition: "settings:zabbix_monitoring == null or settings:zabbix_monitoring.metadata.enabled == false or cluster:net_provider != 'neutron'"
        action: "hide"
        message: "This plugin requires Zabbix plugin"
        strict: false
  community:
    value: "public"
    label: "SNMP community"
    description: "SNMP community for traps authorization"
    weight: 10
    type: "password"
